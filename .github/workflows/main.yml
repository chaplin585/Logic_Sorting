name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:      
            
      - name: Build application
        run:  
          python -m pip install --upgrade pip
          
