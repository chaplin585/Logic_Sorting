name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:  
      - users: actions/checkout@v2     
            
      - name: Build application
        run: 
          make 
          

